# 仓鼠动力机房修订验收 · 2026-09-10

本轮结果：用户指出的布局、物料接入、腿部不动、提示遮挡四类问题已修复。源码/测试、浏览器及本机macOS相关流程已验证；不扩展为Windows或所有公共OS能力的全量验收。

## 输入与对照

- 用户原稿：`docs/design-references/hamster-original.png`，额度左上、跑轮偏右、周边居底，含互动和服饰方向。
- 用户故障截图：`docs/design-references/hamster-hud-obstruction.png`，提示横跨鼠鼠身体。
- 对照页面：`tests/fixtures/hamster-comparison.html`；证据 `output/hamster-v2/reference-comparison.jpg`。
- 最终喂食：`output/hamster-v2/standard-feed.jpg`，440×440；迷你喂食190×190：`mini-feed.jpg`；迷你跑轮：`mini-wheel.jpg`。
- 跑步八帧、七套装扮：`tests/fixtures/hamster-motion.html`、`output/hamster-v2/motion-wardrobe.jpg`。
- 原稿为完整海报，最终按项目透明桌宠规范重排，不宣称房间背景/天气/文字的一比一复刻。金额采用共享人民币额度，不硬编码原稿的Token数量。参考与实现同屏展示，明确尺寸和差异。

## 视觉检查

| 项目 | 问题与修复 | 结果 |
| --- | --- | --- |
| 布局 | 额度回左上；猫、杯子、瓜子、发电机、电线分层；休息姿态左移，发电机缩窄让开脸 | 标准/紧凑/迷你可见 |
| 跑步 | 移除静态腿部扫描线形变；开心/疲惫各四张换脚帧，跑轮与支架独立 | 逐帧看见左右脚伸、抬、落变化；原生运行中看到换脚 |
| 衣服/表情 | 接入七种选择；身体轮廓裁切衣服，保留脸、前爪和脚；疲惫流汗、低额趴下、零额被子 | 逐套及四档截图已检查；睡觉暂收衣服，选择保留 |
| 互动提示 | HUD从身体中部移到窗口82–99%底部独立区；动作时工具行让位 | 18组合DOM测量均不与主体画布相交、不越出窗口 |
| 字体/数值 | 中文短标签、小尺寸保留核心数字/退出/圈数；未知显示—、陈旧标记仍可见 | 190px截图检查；金额可进入详情 |
| 颜色 | 暖色物料，夜间深色提示和额度块，品红抠像 | 未见洋红底或棋盘背景；透明窗口可见桌面 |
| 操作反馈 | 六种原稿互动使用对应道具，短动作自动收尾；持久睡眠可醒来 | 六项开始/取消、Esc睡眠恢复、跑轮六圈完成已实测 |

## 验证分层

### 源码 / 测试

- `npm run build` 通过（Vue/TypeScript检查、Vite、Electron TypeScript编译）。
- `npm test` 50/50 通过。新增短动作时长边界、装扮合法值/独立性、四帧周期与轻柔模式冻结、实际布局常量的HUD不相交检查。
- `git diff --check` 通过。
- 共享账户、额度、设置校验和窗口手势继续复用，没有新增独立查询或凭据服务。

### 浏览器

- 190/300/440px × 白天/夜晚 × ready/stale/signed-out，共18组合。`output/hamster-v2/size-state-matrix.json` 记录HUD位于画布下方、保持在窗口内、额度与原因。未知为—；陈旧仍保留数据待更新。
- 六项原稿互动均进入正确提示，可主动结束/恢复，测试额度保持50%；`output/hamster-v2/action-checks.json`。
- 手动跑轮已从1累计至6圈，完成提示出现后自动回常态。发现轮区中心会误点主体后，改为左轮缘独立命中区并重测。
- 移出窗口时header=hidden、HUD=visible；动作仍可结束。键盘Tab可进入工具；睡眠Esc恢复，面板Esc关闭。
- 模拟页面是开发fixture，不执行真实登录或OS拖窗/托盘。
- 干净页面加载后读取控制台无warn/error。较早反复切换页面的同一测试标签曾记录两条MutationObserver.observe参数错误，项目源码无该MutationObserver调用；新标签加载和互动未复现，不能将旧标签的日志概括为全程无错误。

### macOS 原生与账户

- 以现有Electron应用与现有本机账户启动；已看到透明悬浮窗、真实额度、疲惫跑步、周边和衣橱。
- 原生喂食时底部提示不挡脸，结束入口可见。
- 选择日班装扮后重启应用，原生衣橱仍显示“日班 Value:1”，独立装扮持久化已验证。
- 键盘Tab显示工具；Esc关闭原生衣橱。客户端保持运行。
- 现有真实账户同步正常，未重新走登录；未把浏览器演示数据当真实账户证据。跨日等待、账户过期等仅由源码/测试/fixture覆盖，本轮未等待真实事件。
- 本轮未重做全套OS跨屏拖拽、穿透、托盘恢复/开机启动验收，Windows未运行。旧版浏览器证据在`output/hamster-qa`，不混称本轮验收。
