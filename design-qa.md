# 充气牛马场景验收 · 2026-09-10

final result: passed

验收范围为 Vue 场景、浏览器预览和桌面尺寸 fixture。原生系统跨屏拖动、托盘菜单点击及安装包不在本次运行验收范围；本轮未打包。旧鱼缸报告保存在 [归档](docs/design-qa-aquarium-2026-09-09.md)。

## 参考与实现对照

参考图为 [充气牛马 UI](docs/design-references/charging-buddy.png)，1536 × 1024。实际页面在 http://127.0.0.1:5173/ 使用 1536 × 1024 视口检查，并与参考图一起打开对照；结果见 `output/playwright/buddy/day-final.png`。保留现有应用导航与页脚。额度展示使用现有真实金额模型，预览为 68% / ¥204，参考图的 78% / GB 不作为接口数据。

| 检查面 | 结果 |
| --- | --- |
| 字体 | 沿用中文系统字体，手写提示使用楷体回退；标题、额度、动作标签层次清楚 |
| 布局 | 左侧主场景和附加动作，右侧四档状态、六种互动、五种皮肤，底部道具栏 |
| 色彩 | 天蓝、草绿、奶油白和木牌棕色与参考一致；另检查夜间主题 |
| 物料 | 四张生成图集，含五种皮肤的二十个状态和八种道具；使用精确裁切、透明处理与共同脚底基线 |
| 行为 | 额度状态、切换皮肤、互动反馈、清洁进度、站立/躺下和未连接状态均有对应 UI |

## 已修复的检查发现

- P2：等分图集会切掉部分蹄子，已改为逐格精确矩形和共同基线。
- P2：迷你窗口的文字隐藏规则误隐藏道具图标，已限定选择器；190px 窗口六个动作图标均可见。
- P2：迷你窗口进度文字过小，已设置最小字号。
- P2：面板和 HUD 操作可能触发窗口拖动，已从拖动命中区域排除。
- P2：草地上的提示文字对比度不足，已增加底色并适配夜间主题。

当前无未解决的 P0、P1、P2 问题。

## 运行验证

- 十一种动作分别进入对应状态，取消后返回待机；互动期间额度仍为 68% / ¥204。
- 五种皮肤切换正确，100%、50%、20%、0% 分别使用四档姿态。
- 双击角色可以躺下，站起来按钮恢复；摸摸、开心和驱蚊带局部尾巴摆动。
- 实际拖动清洁覆盖到 78%，自动清洁完成后庆祝并恢复待机。
- 刷新后场景和皮肤选择保留；鱼缸与牛马可来回切换。
- 检查日间、夜间、440px 标准、300px 紧凑及 190px 迷你窗口。迷你换装面板可以滚动并选择末项。
- 300px 减少动态效果模式下，场景动画关闭。
- 未连接 fixture 显示“— / 等待连接”和连接入口。
- 桌面 fixture 收到移动、已移动和结束事件；这不等同于原生系统端到端验证。
- 浏览器检查期间无应用控制台错误或警告。
- `npm run typecheck` 通过；`npm test` 28/28 通过；`git diff --check` 通过。

截图位于 `output/playwright/buddy/`，包括 `day-final.png`、`night-final.png`、`native-standard.png`、`native-compact.png`、`native-mini-clean.png`、`native-mini-wardrobe.png`、`clean-drag.png`、`pink-empty.png` 和 `native-unconnected.png`。

## 实现边界

角色使用栅格姿态、Canvas 局部变形和 CSS 动画，不是三维骨骼模型。进食、饮水使用姿态和道具反馈，嘴部不是逐帧口型；不同生成皮肤有轻微绘制差异，属于 P3。互动不会增加真实额度或修改认证数据。本轮未重启原生应用或执行安装包构建。
